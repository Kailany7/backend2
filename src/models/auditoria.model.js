export default class Auditoria {
    constructor({ nome_colaborador, data, inconsistencia }) {
        this.nome_colaborador = nome_colaborador;
        this.data = data;
        this.inconsistencia = inconsistencia;
    }
}
